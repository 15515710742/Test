//获取任意父元素
var data={
    
}
function getChildren(parent){
    for(var i=0;i<parent.children.length;i++){
        arguemnts.callee(parent.children[i]);
    }
}
window.onload=function(){
    getChildren(document.body);
    
}